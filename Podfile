# Uncomment this line to define a global platform for your project
# platform :ios, '6.0'
use_frameworks!

def testing_pods
    pod 'Quick', '~> 0.3.0'
    pod 'Nimble', '~> 1.0.0'
end
target 'quick-sample' do

end

target 'quick-sampleTests' do
    testing_pods
end

